--- a/getv/port/src/port_render.c
+++ b/getv/port/src/port_render.c
@@ -14,7 +14,7 @@
     static int kb = -1;
     if (kb < 0) {
         const char *e = getenv("GETV_VR_VTXGUARD");
-        kb = (e != NULL && *e != '\0') ? atoi(e) : 0;
+        kb = (e != NULL && *e != '\0') ? atoi(e) : 64 /* ship default ON; dig sets 0 */;
     }
     return kb;
 }
@@ -34,7 +34,7 @@
     static int on = -1;
     if (on < 0) {
         const char *e = getenv("GETV_VR_ADSSIGHT");
-        on = (e != NULL && *e != '\0') ? (atoi(e) != 0) : 0;
+        on = (e != NULL && *e != '\0') ? (atoi(e) != 0) : 1 /* ship default ON; dig sets 0 */;
     }
     return on;
 }
@@ -44,7 +44,7 @@
     static int on = -1;
     if (on < 0) {
         const char *e = getenv("GETV_VR_ADSCULL");
-        on = (e != NULL && *e != '\0') ? (atoi(e) != 0) : 0;
+        on = (e != NULL && *e != '\0') ? (atoi(e) != 0) : 1 /* ship default ON; dig sets 0 */;
     }
     return on;
 }
@@ -54,7 +54,7 @@
     static int on = -1;
     if (on < 0) {
         const char *e = getenv("GETV_XR_HEAD_TRANSLATE");
-        on = (e != NULL && *e != '\0') ? (atoi(e) != 0) : 0;
+        on = (e != NULL && *e != '\0') ? (atoi(e) != 0) : 1 /* ship default ON; dig sets 0 */;
     }
     return on;
 }
@@ -64,7 +64,7 @@
     static int on = -1;
     if (on < 0) {
         const char *e = getenv("GETV_XR_PLAY_AUTORECENTER");
-        on = (e != NULL && *e != '\0') ? (atoi(e) != 0) : 0;
+        on = (e != NULL && *e != '\0') ? (atoi(e) != 0) : 1 /* ship default ON; dig sets 0 */;
     }
     return on;
 }
